{% extends 'base.html.twig' %}

{% block title %}Dashboard - Reporter un rendez-vous{% endblock %}

{% block body %}
<div class="min-h-screen bg-gray-50">
    <div class="flex">
        <!-- Sidebar -->
        {% include '_sidebar.html.twig' %}
        
        <!-- Main Content -->
        <div class="flex-1 lg:ml-8">
            <div class="px-4 py-6 sm:px-6 lg:px-8">
                <!-- Header -->
                <div class="mb-6">
                    <h1 class="text-2xl font-bold text-gray-900">📅 Reporter un rendez-vous</h1>
                    <p class="text-sm text-gray-600">Modifiez la date et/ou le créneau horaire du rendez-vous</p>
                </div>

                <!-- Navigation -->
                <nav class="mb-6" aria-label="Breadcrumb">
                    <ol class="flex items-center space-x-2 text-sm">
                        <li><a href="{{ path('app_dashboard_rendezvous') }}" class="text-gray-500 hover:text-gray-700">Rendez-vous</a></li>
                        <svg class="w-4 h-4 text-gray-400" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd"/></svg>
                        <li class="text-gray-900 font-medium">Reporter</li>
                    </ol>
                </nav>

                <!-- Flash Messages -->
                {% for type, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="mb-6 p-4 text-sm rounded-lg {% if type == 'success' %}text-green-800 bg-green-50 border border-green-200{% elseif type == 'warning' %}text-yellow-800 bg-yellow-50 border border-yellow-200{% else %}text-red-800 bg-red-50 border border-red-200{% endif %}">
                            <div class="flex items-center">
                                {% if type == 'success' %}
                                    <svg class="w-4 h-4 mr-2" fill="currentColor" viewBox="0 0 20 20">
                                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                                    </svg>
                                {% elseif type == 'warning' %}
                                    <svg class="w-4 h-4 mr-2" fill="currentColor" viewBox="0 0 20 20">
                                        <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"/>
                                    </svg>
                                {% else %}
                                    <svg class="w-4 h-4 mr-2" fill="currentColor" viewBox="0 0 20 20">
                                        <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/>
                                    </svg>
                                {% endif %}
                                {{ message }}
                            </div>
                        </div>
                    {% endfor %}
                {% endfor %}

                <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
                    <!-- Informations du rendez-vous actuel -->
                    <div class="lg:col-span-1">
                        <div class="bg-white rounded-lg shadow">
                            <div class="px-6 py-4 border-b border-gray-200">
                                <h2 class="text-lg font-medium text-gray-900">Rendez-vous actuel</h2>
                            </div>
                            <div class="px-6 py-4">
                                <div class="space-y-4">
                                    <div class="flex items-start">
                                        <div class="w-12 h-12 bg-blue-100 rounded-full flex items-center justify-center mr-4">
                                            <svg class="w-6 h-6 text-blue-600" fill="currentColor" viewBox="0 0 20 20">
                                                <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-6-3a2 2 0 11-4 0 2 2 0 014 0zm-2 4a5 5 0 00-4.546 2.916A5.986 5.986 0 0010 16a5.986 5.986 0 004.546-2.084A5 5 0 0010 11z" clip-rule="evenodd"/>
                                            </svg>
                                        </div>
                                        <div>
                                            <p class="text-sm font-medium text-gray-900">Client</p>
                                            <p class="text-sm text-gray-600">{{ rendezvous.user.fullName }}</p>
                                        </div>
                                    </div>
                                    
                                    <div class="flex items-start">
                                        <div class="w-12 h-12 bg-purple-100 rounded-full flex items-center justify-center mr-4">
                                            <svg class="w-6 h-6 text-purple-600" fill="currentColor" viewBox="0 0 20 20">
                                                <path fill-rule="evenodd" d="M6 6V5a3 3 0 013-3h2a3 3 0 013 3v1h2a2 2 0 012 2v6a2 2 0 01-2 2H4a2 2 0 01-2-2V8a2 2 0 012-2h2zm4-3a1 1 0 00-1 1v1h2V4a1 1 0 00-1-1zM4 9v2h2V9H4zm10 0v2h2V9h-2z" clip-rule="evenodd"/>
                                            </svg>
                                        </div>
                                        <div>
                                            <p class="text-sm font-medium text-gray-900">Prestation</p>
                                            <p class="text-sm text-gray-600">{{ rendezvous.prestation.title }}</p>
                                        </div>
                                    </div>

                                    <div class="flex items-start">
                                        <div class="w-12 h-12 bg-green-100 rounded-full flex items-center justify-center mr-4">
                                            <svg class="w-6 h-6 text-green-600" fill="currentColor" viewBox="0 0 20 20">
                                                <path fill-rule="evenodd" d="M6 2a1 1 0 00-1 1v1H4a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V6a2 2 0 00-2-2h-1V3a1 1 0 10-2 0v1H7V3a1 1 0 00-1-1zm0 5a1 1 0 000 2h8a1 1 0 100-2H6z" clip-rule="evenodd"/>
                                            </svg>
                                        </div>
                                        <div>
                                            <p class="text-sm font-medium text-gray-900">Date actuelle</p>
                                            <p class="text-sm text-gray-600">{{ rendezvous.day|date('l j F Y') }}</p>
                                        </div>
                                    </div>

                                    <div class="flex items-start">
                                        <div class="w-12 h-12 bg-orange-100 rounded-full flex items-center justify-center mr-4">
                                            <svg class="w-6 h-6 text-orange-600" fill="currentColor" viewBox="0 0 20 20">
                                                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd"/>
                                            </svg>
                                        </div>
                                        <div>
                                            <p class="text-sm font-medium text-gray-900">Créneau actuel</p>
                                            <p class="text-sm text-gray-600">{{ rendezvous.creneau.startTime|date('H:i') }} - {{ rendezvous.creneau.endTime|date('H:i') }}</p>
                                        </div>
                                    </div>

                                    <div class="flex items-start">
                                        <div class="w-12 h-12 bg-gray-100 rounded-full flex items-center justify-center mr-4">
                                            <svg class="w-6 h-6 text-gray-600" fill="currentColor" viewBox="0 0 20 20">
                                                <path fill-rule="evenodd" d="M3 4a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zm0 4a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zm0 4a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zm0 4a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1z" clip-rule="evenodd"/>
                                            </svg>
                                        </div>
                                        <div>
                                            <p class="text-sm font-medium text-gray-900">Statut</p>
                                            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
                                                {{ rendezvous.status }}
                                            </span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Information importante -->
                        <div class="mt-6 bg-white rounded-lg shadow">
                            <div class="px-6 py-4 border-b border-gray-200">
                                <h2 class="text-lg font-medium text-gray-900">Information</h2>
                            </div>
                            <div class="px-6 py-4">
                                <div class="bg-blue-50 rounded-lg p-4 border border-blue-200">
                                    <div class="flex items-start">
                                        <div class="flex-shrink-0">
                                            <svg class="w-5 h-5 text-blue-600 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                                                <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/>
                                            </svg>
                                        </div>
                                        <div class="ml-3">
                                            <h3 class="text-sm font-medium text-blue-900">Notification automatique</h3>
                                            <p class="text-sm text-blue-700 mt-1">
                                                Le client sera automatiquement averti par email de la modification.
                                            </p>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Formulaire de modification -->
                    <div class="lg:col-span-2">
                        <div class="bg-white rounded-lg shadow">
                            <div class="px-6 py-4 border-b border-gray-200">
                                <h2 class="text-lg font-medium text-gray-900">Nouvelle planification</h2>
                                <p class="text-sm text-gray-500 mt-1">Sélectionnez une nouvelle date et un nouveau créneau</p>
                            </div>
                            <div class="px-6 py-6">
                                {{ form_start(form, {'attr': {'class': 'space-y-6'}}) }}
                                
                                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                                    <!-- Nouvelle date -->
                                    <div class="space-y-4">
                                        <div>
                                            {{ form_label(form.day, 'Nouvelle date', {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-2'}}) }}
                                            <div class="relative">
                                                <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                                    <svg class="h-5 w-5 text-gray-400" fill="currentColor" viewBox="0 0 20 20">
                                                        <path fill-rule="evenodd" d="M6 2a1 1 0 00-1 1v1H4a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V6a2 2 0 00-2-2h-1V3a1 1 0 10-2 0v1H7V3a1 1 0 00-1-1zm0 5a1 1 0 000 2h8a1 1 0 100-2H6z" clip-rule="evenodd"/>
                                                    </svg>
                                                </div>
                                                <input type="text" name="{{ field_name(form.day) }}" value="{{ field_value(form.day) }}" 
                                                       placeholder="Sélectionnez une nouvelle date" 
                                                       class="bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-pink-500 focus:border-pink-500 block w-full pl-10 p-3" 
                                                       id="datepicker" readonly>
                                            </div>
                                            <p class="text-xs text-gray-500 mt-1">Seuls les jours ouvrables (mardi à samedi) sont disponibles</p>
                                        </div>

                                        <!-- Prévisualisation de la date -->
                                        <div id="date-preview" class="hidden bg-green-50 rounded-lg p-3 border border-green-200">
                                            <p class="text-sm text-green-800">
                                                <span class="font-medium">Date sélectionnée :</span>
                                                <span id="selected-date-display"></span>
                                            </p>
                                        </div>
                                    </div>

                                    <!-- Nouveau créneau -->
                                    <div class="space-y-4">
                                        <div>
                                            {{ form_label(form.creneau, 'Nouveau créneau', {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-2'}}) }}
                                            <div id="creneaux-container">
                                                {{ form_widget(form.creneau, {'attr': {'class': 'bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-pink-500 focus:border-pink-500 block w-full p-3'}}) }}
                                            </div>
                                            <p class="text-xs text-gray-500 mt-1">Sélectionnez d'abord une date pour voir les créneaux disponibles</p>
                                        </div>

                                        <!-- Message de disponibilité -->
                                        <div id="availability-message" class="hidden">
                                            <div class="bg-yellow-50 rounded-lg p-3 border border-yellow-200">
                                                <p class="text-sm text-yellow-800">
                                                    <span class="font-medium">Chargement des créneaux...</span>
                                                </p>
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <!-- Actions -->
                                <div class="flex items-center justify-between pt-6 border-t border-gray-200">
                                    <a href="{{ path('app_dashboard_rendezvous') }}" class="inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-lg text-gray-700 bg-white hover:bg-gray-50 transition-colors">
                                        <svg class="w-4 h-4 mr-2" fill="currentColor" viewBox="0 0 20 20">
                                            <path fill-rule="evenodd" d="M7.707 14.707a1 1 0 01-1.414 0L2.586 11l3.707-3.707a1 1 0 011.414 1.414L5.414 11l2.293 2.293a1 1 0 010 1.414z" clip-rule="evenodd"/>
                                        </svg>
                                        Annuler
                                    </a>
                                    <button type="submit" id="submit-btn" class="inline-flex items-center px-6 py-2 bg-pink-600 hover:bg-pink-700 text-white font-medium rounded-lg text-sm transition-colors">
                                        <svg class="w-4 h-4 mr-2" fill="currentColor" viewBox="0 0 20 20">
                                            <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
                                        </svg>
                                        Reporter le rendez-vous
                                    </button>
                                </div>

                                {{ form_widget(form._token) }}
                                {{ form_end(form, {'render_rest': false}) }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
<script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
<script src="https://cdn.jsdelivr.net/npm/flatpickr/dist/l10n/fr.js"></script>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const datePicker = document.getElementById('datepicker');
    const slotsDropdown = document.getElementById('rendezvous_modify_creneau');
    const datePreview = document.getElementById('date-preview');
    const selectedDateDisplay = document.getElementById('selected-date-display');
    const availabilityMessage = document.getElementById('availability-message');
    
    // Initialiser Flatpickr
    const picker = flatpickr("#datepicker", {
        defaultDate: null,
        enableTime: false,
        dateFormat: "Y-m-d",
        altInput: true,
        altFormat: "l j F Y",
        locale: "fr",
        minDate: "today",
        disable: [
            function(date) {
                return (date.getDay() === 1); // Lundi
            },
            function(date) {
                return (date.getDay() === 0); // Dimanche
            }
        ],
        theme: "light",
        onChange: function(selectedDates, dateStr, instance) {
            if (selectedDates.length > 0) {
                const selectedDate = selectedDates[0];
                selectedDateDisplay.textContent = selectedDate.toLocaleDateString('fr-FR', {
                    weekday: 'long',
                    year: 'numeric',
                    month: 'long',
                    day: 'numeric'
                });
                datePreview.classList.remove('hidden');
                
                // Charger les créneaux disponibles
                loadAvailableSlots(dateStr);
            } else {
                datePreview.classList.add('hidden');
                resetSlotsDropdown();
            }
        }
    });

    function loadAvailableSlots(selectedDate) {
        availabilityMessage.classList.remove('hidden');
        availabilityMessage.innerHTML = `
            <div class="bg-yellow-50 rounded-lg p-3 border border-yellow-200">
                <p class="text-sm text-yellow-800">
                    <span class="font-medium">Chargement des créneaux disponibles...</span>
                </p>
            </div>
        `;

        const xhr = new XMLHttpRequest();
        xhr.open('POST', '/get-available-slots', true);
        xhr.setRequestHeader('Content-Type', 'application/x-www-form-urlencoded');
        
        xhr.onreadystatechange = function() {
            if (xhr.readyState === XMLHttpRequest.DONE) {
                if (xhr.status === 200) {
                    const slots = JSON.parse(xhr.responseText);
                    updateSlotsDropdown(slots);
                    
                    if (slots.length > 0) {
                        availabilityMessage.innerHTML = `
                            <div class="bg-green-50 rounded-lg p-3 border border-green-200">
                                <p class="text-sm text-green-800">
                                    <span class="font-medium">${slots.length} créneau${slots.length > 1 ? 'x' : ''} disponible${slots.length > 1 ? 's' : ''}</span>
                                </p>
                            </div>
                        `;
                    } else {
                        availabilityMessage.innerHTML = `
                            <div class="bg-red-50 rounded-lg p-3 border border-red-200">
                                <p class="text-sm text-red-800">
                                    <span class="font-medium">Aucun créneau disponible pour cette date</span>
                                </p>
                            </div>
                        `;
                    }
                } else {
                    availabilityMessage.innerHTML = `
                        <div class="bg-red-50 rounded-lg p-3 border border-red-200">
                            <p class="text-sm text-red-800">
                                <span class="font-medium">Erreur lors du chargement des créneaux</span>
                            </p>
                        </div>
                    `;
                }
            }
        };
        
        xhr.send('date=' + selectedDate);
    }

    function updateSlotsDropdown(slots) {
        slotsDropdown.innerHTML = '<option value="">Sélectionnez un créneau</option>';
        
        slots.forEach(function(slot) {
            const option = document.createElement('option');
            option.value = slot.id;
            option.text = slot.libelle;
            slotsDropdown.appendChild(option);
        });
        
        slotsDropdown.disabled = slots.length === 0;
    }

    function resetSlotsDropdown() {
        slotsDropdown.innerHTML = '<option value="">Sélectionnez d\'abord une date</option>';
        slotsDropdown.disabled = true;
        availabilityMessage.classList.add('hidden');
    }

    // État initial
    resetSlotsDropdown();

    // Validation du formulaire avant soumission
    const form = document.querySelector('form');
    const submitBtn = document.getElementById('submit-btn');

    if (form && submitBtn) {
        form.addEventListener('submit', function(e) {
            const datePicker = document.getElementById('datepicker');
            const slotsDropdown = document.getElementById('rendezvous_modify_creneau');
            
            // Vérifier si une date est sélectionnée
            if (!datePicker.value) {
                e.preventDefault();
                showValidationError('Veuillez sélectionner une nouvelle date pour le rendez-vous.');
                return false;
            }
            
            // Vérifier si un créneau est sélectionné
            if (!slotsDropdown.value) {
                e.preventDefault();
                showValidationError('Veuillez sélectionner un créneau horaire pour le rendez-vous.');
                return false;
            }
            
            return true;
        });
    }

    function showValidationError(message) {
        // Supprimer les messages d'erreur existants
        const existingError = document.getElementById('validation-error');
        if (existingError) {
            existingError.remove();
        }

        // Créer et afficher le message d'erreur
        const errorDiv = document.createElement('div');
        errorDiv.id = 'validation-error';
        errorDiv.className = 'mb-4 p-4 text-sm rounded-lg text-red-800 bg-red-50 border border-red-200';
        errorDiv.innerHTML = `
            <div class="flex items-center">
                <svg class="w-4 h-4 mr-2" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/>
                </svg>
                ${message}
            </div>
        `;
        
        // Insérer le message d'erreur au début du formulaire
        const formContainer = document.querySelector('.bg-white.rounded-lg.shadow .px-6.py-6');
        if (formContainer) {
            formContainer.insertBefore(errorDiv, formContainer.firstChild);
        }
        
        // Faire défiler vers l'erreur
        errorDiv.scrollIntoView({ behavior: 'smooth', block: 'center' });
    }
});
</script>

<style>
.flatpickr-calendar {
    font-family: inherit;
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
    border: 1px solid #e5e7eb;
}

.flatpickr-day.today {
    background: #fef3f2;
    border-color: #ec4899;
    color: #ec4899;
}

.flatpickr-day.selected {
    background: #ec4899;
    border-color: #ec4899;
}

.flatpickr-day:hover {
    background: #fdf2f8;
}
</style>
{% endblock %}