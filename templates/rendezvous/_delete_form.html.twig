<form method="post" action="{{ path('app_rendezvous_delete', {'id': rendezvous.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rendezvous.id) }}">
    <button class="btn">Delete</button>
</form>
