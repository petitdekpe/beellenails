{% extends 'base.html.twig' %}

{% block title %}CategoryPrestation{% endblock %}

{% block body %}
    <h1>CategoryPrestation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_prestation.id }}</td>
            </tr>
            <tr>
                <th>NomCategory</th>
                <td>{{ category_prestation.NomCategory }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ category_prestation.imageName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_category_prestation_index') }}">back to list</a>

    <a href="{{ path('app_category_prestation_edit', {'id': category_prestation.id}) }}">edit</a>

    {{ include('category_prestation/_delete_form.html.twig') }}
{% endblock %}
