<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Certificat de Formation</title>
    <style>
        @page {
            size: A4 landscape;
            margin: 0;
        }
        
        body {
            margin: 0;
            padding: 0;
            font-family: 'Georgia', 'Times New Roman', serif;
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 50%, #cbd5e1 100%);
            width: 297mm;
            height: 210mm;
            position: relative;
            overflow: hidden;
        }
        
        .certificate-container {
            width: 100%;
            height: 100%;
            position: relative;
            border: 15px solid #1e293b;
            box-sizing: border-box;
            background: white;
        }
        
        .certificate-inner {
            width: 100%;
            height: 100%;
            position: relative;
            border: 8px solid #ec4899;
            box-sizing: border-box;
            padding: 30px;
            background: 
                radial-gradient(circle at 20% 20%, rgba(236, 72, 153, 0.05) 0%, transparent 50%),
                radial-gradient(circle at 80% 80%, rgba(139, 92, 246, 0.05) 0%, transparent 50%),
                linear-gradient(135deg, #ffffff 0%, #f8fafc 100%);
        }
        
        .decorative-corner {
            position: absolute;
            width: 60px;
            height: 60px;
            border: 3px solid #ec4899;
        }
        
        .corner-tl {
            top: 15px;
            left: 15px;
            border-right: none;
            border-bottom: none;
        }
        
        .corner-tr {
            top: 15px;
            right: 15px;
            border-left: none;
            border-bottom: none;
        }
        
        .corner-bl {
            bottom: 15px;
            left: 15px;
            border-right: none;
            border-top: none;
        }
        
        .corner-br {
            bottom: 15px;
            right: 15px;
            border-left: none;
            border-top: none;
        }
        
        .header {
            text-align: center;
            margin-bottom: 40px;
        }
        
        .logo {
            font-size: 36px;
            font-weight: bold;
            color: #ec4899;
            margin-bottom: 10px;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.1);
        }
        
        .certificate-title {
            font-size: 48px;
            font-weight: bold;
            color: #1e293b;
            margin: 20px 0;
            text-transform: uppercase;
            letter-spacing: 4px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.1);
        }
        
        .subtitle {
            font-size: 20px;
            color: #64748b;
            margin-bottom: 30px;
            font-style: italic;
        }
        
        .main-content {
            text-align: center;
            margin: 40px 0;
        }
        
        .certification-text {
            font-size: 22px;
            color: #374151;
            margin-bottom: 20px;
            line-height: 1.6;
        }
        
        .recipient-name {
            font-size: 42px;
            font-weight: bold;
            color: #ec4899;
            margin: 30px 0;
            text-decoration: underline;
            text-decoration-color: #8b5cf6;
            text-decoration-thickness: 3px;
            text-underline-offset: 8px;
        }
        
        .formation-name {
            font-size: 28px;
            font-weight: bold;
            color: #1e293b;
            margin: 25px 0;
            font-style: italic;
        }
        
        .completion-details {
            margin: 30px 0;
            font-size: 16px;
            color: #6b7280;
        }
        
        .completion-date {
            font-size: 18px;
            color: #374151;
            font-weight: 600;
        }
        
        .footer {
            position: absolute;
            bottom: 40px;
            left: 50%;
            transform: translateX(-50%);
            width: 100%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 60px;
            box-sizing: border-box;
        }
        
        .signature-section {
            text-align: center;
            flex: 1;
        }
        
        .signature-line {
            border-bottom: 2px solid #374151;
            width: 200px;
            margin: 20px auto 10px auto;
        }
        
        .signature-title {
            font-size: 14px;
            color: #6b7280;
            font-weight: 600;
        }
        
        .certificate-id {
            position: absolute;
            bottom: 10px;
            right: 20px;
            font-size: 12px;
            color: #9ca3af;
            font-family: monospace;
        }
        
        .verification-qr {
            position: absolute;
            bottom: 20px;
            left: 20px;
            width: 60px;
            height: 60px;
            border: 2px solid #e5e7eb;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 10px;
            color: #9ca3af;
            text-align: center;
        }
        
        .achievement-badge {
            position: absolute;
            top: 80px;
            right: 60px;
            width: 80px;
            height: 80px;
            border-radius: 50%;
            background: linear-gradient(135deg, #fbbf24, #f59e0b);
            border: 4px solid #92400e;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 36px;
            color: white;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.3);
        }
        
        .skills-section {
            margin: 25px 0;
            font-size: 16px;
            color: #4b5563;
            text-align: center;
        }
        
        .skills-title {
            font-weight: bold;
            margin-bottom: 10px;
            color: #374151;
        }
        
        .decorative-flourish {
            text-align: center;
            font-size: 24px;
            color: #ec4899;
            margin: 20px 0;
        }
        
        .watermark {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(-45deg);
            font-size: 120px;
            color: rgba(236, 72, 153, 0.03);
            font-weight: bold;
            z-index: 0;
            pointer-events: none;
        }
        
        .content-overlay {
            position: relative;
            z-index: 1;
        }
    </style>
</head>
<body>
    <div class="certificate-container">
        <div class="certificate-inner">
            <!-- Decorative corners -->
            <div class="decorative-corner corner-tl"></div>
            <div class="decorative-corner corner-tr"></div>
            <div class="decorative-corner corner-bl"></div>
            <div class="decorative-corner corner-br"></div>
            
            <!-- Achievement badge -->
            <div class="achievement-badge">🏆</div>
            
            <!-- Watermark -->
            <div class="watermark">BELLENAILS</div>
            
            <div class="content-overlay">
                <!-- Header -->
                <div class="header">
                    <div class="logo">💅 BelleNails Academy</div>
                    <div class="certificate-title">Certificat</div>
                    <div class="subtitle">de Réussite Professionnelle</div>
                </div>
                
                <!-- Main content -->
                <div class="main-content">
                    <div class="certification-text">
                        Il est certifié par les présentes que
                    </div>
                    
                    <div class="recipient-name">
                        {{ enrollment.user.prenom }} {{ enrollment.user.nom }}
                    </div>
                    
                    <div class="certification-text">
                        a suivi avec succès et terminé la formation
                    </div>
                    
                    <div class="formation-name">
                        "{{ enrollment.formation.nom }}"
                    </div>
                    
                    <div class="decorative-flourish">❋ ❋ ❋</div>
                    
                    {% if enrollment.formation.level %}
                    <div class="skills-section">
                        <div class="skills-title">Niveau de Formation</div>
                        <div>{{ enrollment.formation.level|capitalize }}</div>
                    </div>
                    {% endif %}
                    
                    <div class="completion-details">
                        Formation complétée à <strong>{{ enrollment.progressPercentage }}%</strong><br>
                        Durée totale d'étude : <strong>{{ ((enrollment.totalTimeSpent / 60) > 60) ? ((enrollment.totalTimeSpent / 3600)|round(1)) ~ 'h' : ((enrollment.totalTimeSpent / 60)|round) ~ 'min' }}</strong>
                    </div>
                    
                    <div class="completion-date">
                        Délivrée le {{ enrollment.completedAt|date('d F Y') }}
                    </div>
                </div>
                
                <!-- Footer -->
                <div class="footer">
                    <div class="signature-section">
                        <div class="signature-line"></div>
                        <div class="signature-title">Directrice Pédagogique</div>
                    </div>
                    
                    <div class="signature-section">
                        <div class="signature-line"></div>
                        <div class="signature-title">BelleNails Academy</div>
                    </div>
                </div>
                
                <!-- Certificate ID -->
                <div class="certificate-id">
                    Certificat N° {{ enrollment.id|str_pad(6, '0') }}-{{ enrollment.completedAt|date('Y') }}
                </div>
                
                <!-- QR Code placeholder -->
                <div class="verification-qr">
                    QR<br>Vérification
                </div>
            </div>
        </div>
    </div>
</body>
</html>