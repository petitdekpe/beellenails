<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liste des rendez-vous pour demain</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }
        h1 {
            text-align: center;
            margin-bottom: 20px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            padding: 10px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        th {
            background-color: #f2f2f2;
            font-weight: bold;
        }
        tr:hover {
            background-color: #f5f5f5;
        }
    </style>
</head>
<body>
    <h1>Liste des rendez-vous pour demain</h1>
    <table>
        <thead>
            <tr>
                <th>Utilisateur</th>
                <th>Créneau</th>
                <th>Prestation</th>
            </tr>
        </thead>
        <tbody>
            {% for appointment in appointments %}
                <tr>
                    <td>{{ appointment.user.prenom }} {{ appointment.user.nom }}</td>
                    <td>{{ appointment.creneau.startTime|date('H:i') }} - {{ appointment.creneau.endTime|date('H:i') }}</td>
                    <td>{{ appointment.prestation.title }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>
