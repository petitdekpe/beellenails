{% extends 'emails/base_email.html.twig' %}

{% block title %}Rendez-vous annulé - BeElle Nails{% endblock %}

{% block preheader %}Un rendez-vous a été annulé par un client.{% endblock %}

{% block header_title %}Rendez-vous Annulé ❌{% endblock %}

{% block header_subtitle %}Notification administrateur{% endblock %}

{% block content %}
    <div class="greeting">
        Bonjour Administrateur ! 👋
    </div>

    <div class="content-text">
        Nous vous informons qu'un rendez-vous a été <strong>annulé par le client</strong> sur votre plateforme BeElle Nails Care.
    </div>

    <div class="info-box">
        <div class="info-title">
            Rendez-vous annulé
        </div>
        <div class="info-item">
            <span class="info-label">Client :</span>
            <span class="info-value">{{ rendezvous.user.prenom }} {{ rendezvous.user.nom }}</span>
        </div>
        <div class="info-item">
            <span class="info-label">Email :</span>
            <span class="info-value">{{ rendezvous.user.email }}</span>
        </div>
        <div class="info-item">
            <span class="info-label">Téléphone :</span>
            <span class="info-value">{{ rendezvous.user.telephone|default('Non renseigné') }}</span>
        </div>
        <div class="info-item">
            <span class="info-label">Prestation :</span>
            <span class="info-value">{{ rendezvous.prestation.title }}</span>
        </div>
        <div class="info-item">
            <span class="info-label">Date prévue :</span>
            <span class="info-value">{{ rendezvous.day|date('d/m/Y') }}</span>
        </div>
        <div class="info-item">
            <span class="info-label">Heure prévue :</span>
            <span class="info-value">{{ rendezvous.creneau.startTime|date('H:i') }}</span>
        </div>
        {% if rendezvous.totalCost %}
        <div class="info-item">
            <span class="info-label">Coût total :</span>
            <span class="info-value">{{ rendezvous.totalCost }} FCFA</span>
        </div>
        {% endif %}
        <div class="info-item">
            <span class="info-label">Statut :</span>
            <span class="info-value">{{ rendezvous.status }}</span>
        </div>
    </div>

    <div class="content-text">
        <strong>Actions à prévoir :</strong><br>
        • Le créneau est maintenant disponible pour d'autres clients<br>
        • Vérifier s'il y a une liste d'attente<br>
        • Gérer le remboursement si un paiement a été effectué<br>
        • Mettre à jour votre planning
    </div>

    <div class="btn-container">
        <a href="https://beellenails.com/dashboard" class="btn-primary">
            Voir le dashboard
        </a>
    </div>

    <div class="content-text">
        Annulation effectuée le : <strong>{{ rendezvous.updatedAt|date('d/m/Y à H:i') }}</strong>
    </div>

    <div class="content-text">
        <em>Notification automatique du système BeElle Nails Care</em><br>
        <strong>L'équipe BeElle</strong>
    </div>
{% endblock %}