{% extends 'emails/base_email.html.twig' %}

{% block title %}Rendez-vous modifié - BeElle Nails{% endblock %}

{% block preheader %}Un rendez-vous a été modifié par un client.{% endblock %}

{% block header_title %}Rendez-vous Modifié 📝{% endblock %}

{% block header_subtitle %}Notification administrateur{% endblock %}

{% block content %}
    <div class="greeting">
        Bonjour Administrateur ! 👋
    </div>

    <div class="content-text">
        Nous vous informons qu'un rendez-vous a été <strong>modifié par le client</strong> sur votre plateforme BeElle Nails Care.
        Voici les nouvelles informations :
    </div>

    <div class="info-box">
        <div class="info-title">
            Nouvelles informations du rendez-vous
        </div>
        <div class="info-item">
            <span class="info-label">Client :</span>
            <span class="info-value">{{ rendezvous.user.prenom }} {{ rendezvous.user.nom }}</span>
        </div>
        <div class="info-item">
            <span class="info-label">Email :</span>
            <span class="info-value">{{ rendezvous.user.email }}</span>
        </div>
        <div class="info-item">
            <span class="info-label">Téléphone :</span>
            <span class="info-value">{{ rendezvous.user.telephone|default('Non renseigné') }}</span>
        </div>
        <div class="info-item">
            <span class="info-label">Prestation :</span>
            <span class="info-value">{{ rendezvous.prestation.title }}</span>
        </div>
        <div class="info-item">
            <span class="info-label">Nouvelle date :</span>
            <span class="info-value">{{ rendezvous.day|date('d/m/Y') }}</span>
        </div>
        <div class="info-item">
            <span class="info-label">Nouvel horaire :</span>
            <span class="info-value">{{ rendezvous.creneau.startTime|date('H:i') }}</span>
        </div>
        {% if rendezvous.totalCost %}
        <div class="info-item">
            <span class="info-label">Coût total :</span>
            <span class="info-value">{{ rendezvous.totalCost }} FCFA</span>
        </div>
        {% endif %}
        <div class="info-item">
            <span class="info-label">Statut :</span>
            <span class="info-value">{{ rendezvous.status }}</span>
        </div>
    </div>

    <div class="content-text">
        <strong>Actions à prévoir :</strong><br>
        • Vérifier la disponibilité du nouveau créneau<br>
        • Mettre à jour votre planning personnel<br>
        • Préparer le matériel nécessaire<br>
        • L'ancien créneau est maintenant libre pour d'autres clients<br>
        • Vérifier s'il y a une liste d'attente pour l'ancien créneau
    </div>

    <div class="btn-container">
        <a href="https://beellenails.com/dashboard" class="btn-primary">
            Voir le dashboard
        </a>
    </div>

    <div class="content-text">
        Modification effectuée le : <strong>{{ rendezvous.updatedAt|date('d/m/Y à H:i') }}</strong>
    </div>

    <div class="content-text">
        <em>Notification automatique du système BeElle Nails Care</em><br>
        <strong>L'équipe BeElle</strong>
    </div>
{% endblock %}