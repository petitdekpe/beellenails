{% extends 'dashboard.html.twig' %}

{% block title %}{{ formation.Nom }} - Détails{% endblock %}

{% block content %}
<div class="space-y-6">
    <!-- Header avec actions -->
    <div class="flex justify-between items-center">
        <div class="flex items-center">
            <a href="{{ path('app_dashboard_formations_index') }}" class="text-gray-600 hover:text-gray-800 mr-4">
                <i class="fas fa-arrow-left"></i> Retour
            </a>
            <h1 class="text-2xl font-bold text-gray-900">📚 {{ formation.Nom }}</h1>
        </div>
        <div class="flex space-x-3">
            <a href="{{ path('app_dashboard_formation_edit', {id: formation.id}) }}" class="inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-yellow-600 hover:bg-yellow-700">
                <i class="fas fa-edit mr-2"></i> Modifier
            </a>
            <a href="{{ path('app_dashboard_formation_modules', {id: formation.id}) }}" class="inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700">
                <i class="fas fa-list mr-2"></i> Modules ({{ formation.modules|length }})
            </a>
            <a href="{{ path('app_dashboard_formation_enrollments', {id: formation.id}) }}" class="inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-green-600 hover:bg-green-700">
                <i class="fas fa-users mr-2"></i> Inscriptions ({{ stats.totalEnrollments }})
            </a>
        </div>
    </div>

    <!-- Statistiques principales -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        <div class="bg-white rounded-lg shadow-sm border border-l-4 border-l-blue-500 p-6">
            <div class="flex items-center justify-between">
                <div>
                    <div class="text-xs font-semibold text-blue-600 uppercase mb-1">Total Inscriptions</div>
                    <div class="text-2xl font-bold text-gray-900">{{ stats.totalEnrollments }}</div>
                </div>
                <div class="text-gray-400">
                    <i class="fas fa-users fa-2x"></i>
                </div>
            </div>
        </div>

        <div class="bg-white rounded-lg shadow-sm border border-l-4 border-l-green-500 p-6">
            <div class="flex items-center justify-between">
                <div>
                    <div class="text-xs font-semibold text-green-600 uppercase mb-1">Inscriptions Actives</div>
                    <div class="text-2xl font-bold text-gray-900">{{ stats.activeEnrollments }}</div>
                </div>
                <div class="text-gray-400">
                    <i class="fas fa-play-circle fa-2x"></i>
                </div>
            </div>
        </div>

        <div class="bg-white rounded-lg shadow-sm border border-l-4 border-l-cyan-500 p-6">
            <div class="flex items-center justify-between">
                <div>
                    <div class="text-xs font-semibold text-cyan-600 uppercase mb-1">Taux de Complétion</div>
                    <div class="text-2xl font-bold text-gray-900">{{ stats.avgCompletion }}%</div>
                </div>
                <div class="text-gray-400">
                    <i class="fas fa-percentage fa-2x"></i>
                </div>
            </div>
        </div>

        <div class="bg-white rounded-lg shadow-sm border border-l-4 border-l-yellow-500 p-6">
            <div class="flex items-center justify-between">
                <div>
                    <div class="text-xs font-semibold text-yellow-600 uppercase mb-1">Revenus</div>
                    <div class="text-2xl font-bold text-gray-900">{{ stats.revenue }} F CFA</div>
                </div>
                <div class="text-gray-400">
                    <i class="fas fa-coins fa-2x"></i>
                </div>
            </div>
        </div>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
        <!-- Informations de la formation -->
        <div class="lg:col-span-2">
            <div class="bg-white rounded-lg shadow-sm border">
                <div class="px-6 py-4 border-b border-gray-200">
                    <h2 class="text-lg font-semibold text-gray-900">Informations de la Formation</h2>
                </div>
                <div class="p-6">
                    <div class="{% if formation.imageName %}grid grid-cols-1 md:grid-cols-3 gap-6{% endif %}">
                        {% if formation.imageName %}
                        <div>
                            <img src="{{ vich_uploader_asset(formation, 'image') }}" 
                                 alt="{{ formation.Nom }}" class="w-full rounded-lg">
                        </div>
                        {% endif %}
                        <div class="{% if formation.imageName %}md:col-span-2{% endif %}">
                            <dl class="space-y-4">
                                <div>
                                    <dt class="text-sm font-medium text-gray-500">Nom :</dt>
                                    <dd class="mt-1 text-sm text-gray-900">{{ formation.Nom }}</dd>
                                </div>
                                <div>
                                    <dt class="text-sm font-medium text-gray-500">Niveau :</dt>
                                    <dd class="mt-1 text-sm text-gray-900">
                                        {% if formation.level %}
                                            <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-blue-100 text-blue-800">{{ formation.level|capitalize }}</span>
                                        {% else %}
                                            <span class="text-gray-500">Non défini</span>
                                        {% endif %}
                                    </dd>
                                </div>
                                <div>
                                    <dt class="text-sm font-medium text-gray-500">Type :</dt>
                                    <dd class="mt-1 text-sm text-gray-900">
                                        {% if formation.isFree %}
                                            <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-green-100 text-green-800">Gratuite</span>
                                        {% else %}
                                            <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-blue-100 text-blue-800">Payante - {{ formation.Cout }} F CFA</span>
                                        {% endif %}
                                    </dd>
                                </div>
                                <div>
                                    <dt class="text-sm font-medium text-gray-500">Durée :</dt>
                                    <dd class="mt-1 text-sm text-gray-900">{{ formation.duration ?? 'Non définie' }}</dd>
                                </div>
                                <div>
                                    <dt class="text-sm font-medium text-gray-500">Statut :</dt>
                                    <dd class="mt-1 text-sm text-gray-900">
                                        <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium {{ formation.isActive ? 'bg-green-100 text-green-800' : 'bg-gray-100 text-gray-800' }}">
                                            {{ formation.isActive ? 'Active' : 'Inactive' }}
                                        </span>
                                    </dd>
                                </div>
                                <div>
                                    <dt class="text-sm font-medium text-gray-500">Créée le :</dt>
                                    <dd class="mt-1 text-sm text-gray-900">{{ formation.createdAt|date('d/m/Y à H:i') }}</dd>
                                </div>
                                <div>
                                    <dt class="text-sm font-medium text-gray-500">Mise à jour :</dt>
                                    <dd class="mt-1 text-sm text-gray-900">{{ formation.updatedAt|date('d/m/Y à H:i') }}</dd>
                                </div>
                            </dl>
                        </div>
                    </div>
                    
                    {% if formation.Description %}
                    <div class="mt-6">
                        <h3 class="text-sm font-medium text-gray-900 mb-2">Description :</h3>
                        <div class="border border-gray-200 p-4 rounded-lg bg-gray-50">
                            {{ formation.Description|nl2br }}
                        </div>
                    </div>
                    {% endif %}

                    {% if formation.Objectif %}
                    <div class="mt-6">
                        <h3 class="text-sm font-medium text-gray-900 mb-2">Objectifs pédagogiques :</h3>
                        <div class="border border-gray-200 p-4 rounded-lg bg-gray-50">
                            {{ formation.Objectif|nl2br }}
                        </div>
                    </div>
                    {% endif %}
                </div>
            </div>

            <!-- Modules de la formation -->
            <div class="bg-white rounded-lg shadow-sm border">
                <div class="px-6 py-4 border-b border-gray-200 flex justify-between items-center">
                    <h2 class="text-lg font-semibold text-gray-900">Modules ({{ formation.modules|length }})</h2>
                    <a href="{{ path('app_dashboard_formation_module_new', {id: formation.id}) }}" class="inline-flex items-center px-3 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700">
                        <i class="fas fa-plus mr-2"></i> Nouveau Module
                    </a>
                </div>
                <div class="p-6">
                    {% if formation.modules is not empty %}
                        <div class="space-y-4">
                            {% for module in formation.modules %}
                            <div class="border border-gray-200 rounded-lg p-4 hover:bg-gray-50 transition-colors">
                                <div class="flex justify-between items-start">
                                    <div class="flex-1">
                                        <h3 class="text-lg font-medium text-gray-900 mb-2">{{ module.position }}. {{ module.title }}</h3>
                                        {% if module.description %}
                                            <p class="text-gray-600 mb-2">{{ module.description|slice(0, 100) }}...</p>
                                        {% endif %}
                                        <div class="flex items-center space-x-4 text-sm text-gray-500">
                                            {% if module.duration %}⏱️ {{ module.formattedDuration }}{% endif %}
                                            {% if module.youtubeUrl %}📹 Vidéo{% endif %}
                                        </div>
                                    </div>
                                    <div class="flex space-x-2 ml-4">
                                        <button class="inline-flex items-center px-3 py-2 border border-gray-300 shadow-sm text-sm leading-4 font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50">
                                            <i class="fas fa-edit mr-1"></i> Modifier
                                        </button>
                                        <button class="inline-flex items-center px-3 py-2 border border-gray-300 shadow-sm text-sm leading-4 font-medium rounded-md text-red-600 bg-white hover:bg-red-50">
                                            <i class="fas fa-trash mr-1"></i> Supprimer
                                        </button>
                                    </div>
                                </div>
                            </div>
                            {% endfor %}
                        </div>
                    {% else %}
                        <div class="text-center py-12">
                            <div class="mx-auto h-24 w-24 text-gray-300 mb-4">
                                <i class="fas fa-list text-6xl"></i>
                            </div>
                            <h3 class="text-lg font-medium text-gray-900 mb-2">Aucun module créé</h3>
                            <p class="text-gray-500 mb-6">Cette formation n'a pas encore de modules.</p>
                            <a href="{{ path('app_dashboard_formation_module_new', {id: formation.id}) }}" class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700">
                                <i class="fas fa-plus mr-2"></i>Créer le premier module
                            </a>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>

        <!-- Sidebar -->
        <div>
            <!-- Actions rapides -->
            <div class="bg-white rounded-lg shadow-sm border">
                <div class="px-6 py-4 border-b border-gray-200">
                    <h2 class="text-lg font-semibold text-gray-900">Actions Rapides</h2>
                </div>
                <div class="p-6">
                    <div class="space-y-3">
                        <button class="w-full inline-flex justify-center items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white {{ formation.isActive ? 'bg-yellow-600 hover:bg-yellow-700' : 'bg-green-600 hover:bg-green-700' }} toggle-status" 
                                data-formation-id="{{ formation.id }}">
                            <i class="fas fa-{{ formation.isActive ? 'pause' : 'play' }} mr-2"></i>
                            {{ formation.isActive ? 'Désactiver' : 'Activer' }} la formation
                        </button>
                        
                        <a href="{{ path('app_formation_show', {id: formation.id}) }}" class="w-full inline-flex justify-center items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700" target="_blank">
                            <i class="fas fa-external-link-alt mr-2"></i> Voir côté client
                        </a>
                        
                        <button class="w-full inline-flex justify-center items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50" onclick="copyFormationLink()">
                            <i class="fas fa-copy mr-2"></i> Copier le lien
                        </button>
                        
                        <div class="relative">
                            <button class="w-full inline-flex justify-center items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-purple-600 hover:bg-purple-700" type="button">
                                <i class="fas fa-user-plus mr-2"></i> Inscription manuelle
                            </button>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Statistiques détaillées -->
            <div class="bg-white rounded-lg shadow-sm border mt-6">
                <div class="px-6 py-4 border-b border-gray-200">
                    <h2 class="text-lg font-semibold text-gray-900">Statistiques Détaillées</h2>
                </div>
                <div class="p-6">
                    <div class="chart-container" style="height: 200px;">
                        <canvas id="completionChart"></canvas>
                    </div>
                    
                    <div class="border-t border-gray-200 mt-6 pt-6">
                        <div class="grid grid-cols-2 gap-6 text-center">
                            <div>
                                <div class="p-3">
                                    <h3 class="text-2xl font-semibold text-green-600">{{ stats.completedEnrollments }}</h3>
                                    <p class="text-sm text-gray-500">Terminées</p>
                                </div>
                            </div>
                            <div>
                                <div class="p-3">
                                    <h3 class="text-2xl font-semibold text-yellow-600">{{ stats.expiredEnrollments }}</h3>
                                    <p class="text-sm text-gray-500">Expirées</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Ressources -->
            {% if formation.resources is not empty %}
            <div class="bg-white rounded-lg shadow-sm border mt-6">
                <div class="px-6 py-4 border-b border-gray-200">
                    <h2 class="text-lg font-semibold text-gray-900">Ressources ({{ formation.resources|length }})</h2>
                </div>
                <div class="p-6">
                    {% for resource in formation.resources %}
                    <div class="flex items-center mb-3">
                        <span class="mr-3 text-gray-500">{{ resource.typeIcon }}</span>
                        <div class="flex-1">
                            <p class="text-sm font-medium text-gray-900">{{ resource.title }}</p>
                            <p class="text-sm text-gray-500">{{ resource.formattedFileSize }}</p>
                        </div>
                    </div>
                    {% endfor %}
                </div>
            </div>
            {% endif %}
        </div>
    </div>
</div>

<!-- Modal inscription utilisateur -->
<div class="fixed inset-0 bg-gray-600 bg-opacity-50 overflow-y-auto h-full w-full hidden" id="enrollUserModal">
    <div class="relative top-20 mx-auto p-5 border w-96 shadow-lg rounded-md bg-white">
        <div class="mt-3">
            <div class="flex justify-between items-center mb-4">
                <h3 class="text-lg font-medium text-gray-900">Inscrire un utilisateur</h3>
                <button type="button" class="text-gray-400 hover:text-gray-600" onclick="closeModal('enrollUserModal')">
                    <span class="sr-only">Fermer</span>
                    <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                    </svg>
                </button>
            </div>
            <form id="enrollUserForm">
                <div class="space-y-4">
                    <div>
                        <label for="userSelect" class="block text-sm font-medium text-gray-700">Utilisateur</label>
                        <select class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500" id="userSelect" name="userId" required>
                            <option value="">Sélectionner un utilisateur...</option>
                            <!-- Options chargées via AJAX -->
                        </select>
                    </div>
                    
                    <div>
                        <label for="accessType" class="block text-sm font-medium text-gray-700">Type d'accès</label>
                        <select class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500" id="accessType" name="accessType">
                            <option value="30_days">30 jours</option>
                            <option value="60_days">60 jours</option>
                            <option value="90_days">90 jours</option>
                            <option value="lifetime">Accès à vie</option>
                        </select>
                    </div>
                </div>
                <div class="mt-6 flex justify-end space-x-3">
                    <button type="button" class="px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50" onclick="closeModal('enrollUserModal')">Annuler</button>
                    <button type="submit" class="px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700">Inscrire</button>
                </div>
            </form>
        </div>
    </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
    // Graphique de complétion
    const ctx = document.getElementById('completionChart')?.getContext('2d');
    if (ctx) {
        new Chart(ctx, {
            type: 'doughnut',
            data: {
                labels: ['Actives', 'Terminées', 'Expirées'],
                datasets: [{
                    data: [{{ stats.activeEnrollments }}, {{ stats.completedEnrollments }}, {{ stats.expiredEnrollments }}],
                    backgroundColor: ['#0891b2', '#059669', '#d97706']
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        display: false
                    }
                }
            }
        });
    }
    
    // Toggle statut formation
    const toggleButton = document.querySelector('.toggle-status');
    if (toggleButton) {
        toggleButton.addEventListener('click', function() {
            const formationId = this.dataset.formationId;
            
            fetch(`/dashboard/formations/${formationId}/toggle-status`, {
                method: 'POST',
                headers: {
                    'X-Requested-With': 'XMLHttpRequest'
                }
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    location.reload();
                }
            })
            .catch(error => {
                console.error('Erreur:', error);
            });
        });
    }
});

function closeModal(modalId) {
    document.getElementById(modalId).classList.add('hidden');
}

function copyFormationLink() {
    const url = "{{ url('app_formation_show', {id: formation.id}) }}";
    navigator.clipboard.writeText(url).then(function() {
        showAlert('success', 'Lien copié dans le presse-papiers !');
    }).catch(function() {
        showAlert('error', 'Erreur lors de la copie du lien');
    });
}

function showAlert(type, message) {
    const bgClass = type === 'success' ? 'bg-green-100 border-green-400 text-green-700' : 'bg-red-100 border-red-400 text-red-700';
    const alertHtml = `
        <div class="${bgClass} px-4 py-3 rounded border mb-4 relative" role="alert">
            <span class="block sm:inline">${message}</span>
            <span class="absolute top-0 bottom-0 right-0 px-4 py-3 cursor-pointer" onclick="this.parentElement.remove()">
                <svg class="fill-current h-6 w-6" role="button" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20">
                    <title>Close</title>
                    <path d="M14.348 14.849a1.2 1.2 0 0 1-1.697 0L10 11.819l-2.651 3.029a1.2 1.2 0 1 1-1.697-1.697l2.758-3.15-2.759-3.152a1.2 1.2 0 1 1 1.697-1.697L10 8.183l2.651-3.031a1.2 1.2 0 1 1 1.697 1.697l-2.758 3.152 2.758 3.15a1.2 1.2 0 0 1 0 1.698z"/>
                </svg>
            </span>
        </div>
    `;
    
    const container = document.querySelector('.space-y-6');
    container.insertAdjacentHTML('afterbegin', alertHtml);
    
    setTimeout(function() {
        const alert = container.querySelector('[role="alert"]');
        if (alert) {
            alert.remove();
        }
    }, 5000);
}
</script>

<!-- Chart.js -->
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
{% endblock %}