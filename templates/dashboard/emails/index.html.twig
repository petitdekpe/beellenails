{% extends 'dashboard.html.twig' %}

{% block title %}Dashboard - Envoi d'emails{% endblock %}

{% block content %}
<div class="max-w-7xl mx-auto">
        
        <!-- Main Content -->
        <div 
            <div class="px-4 py-6 sm:px-6 lg:px-8">
                <!-- Header -->
                <div class="mb-6">
                    <h1 class="text-2xl font-bold text-gray-900">📧 Envoi d'emails aux clients</h1>
                    <p class="text-sm text-gray-600">{{ total_clients }} clients enregistrés</p>
                </div>

                <!-- Messages flash -->
                {% for message in app.flashes('success') %}
                    <div class="mb-4 bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded relative">
                        {{ message }}
                    </div>
                {% endfor %}

                {% for message in app.flashes('error') %}
                    <div class="mb-4 bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded relative">
                        {{ message }}
                    </div>
                {% endfor %}

                <!-- Formulaire d'envoi d'email -->
                <div class="bg-white rounded-lg shadow">
                    <div class="px-6 py-4 border-b border-gray-200">
                        <h2 class="text-lg font-medium text-gray-900">Composer un email</h2>
                    </div>
                    <div class="px-6 py-6">
                        {{ form_start(form, {'attr': {'class': 'space-y-6'}}) }}
                        
                        <!-- Objet -->
                        <div>
                            {{ form_label(form.subject, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'}}) }}
                            {{ form_widget(form.subject) }}
                            {{ form_errors(form.subject) }}
                        </div>

                        <!-- Message -->
                        <div>
                            {{ form_label(form.message, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'}}) }}
                            {{ form_widget(form.message) }}
                            {{ form_errors(form.message) }}
                            <div class="mt-2 p-3 bg-blue-50 rounded-lg border border-blue-200">
                                <p class="text-xs font-medium text-blue-800 mb-2">💡 Balises de personnalisation disponibles :</p>
                                <div class="grid grid-cols-2 gap-2 text-xs text-blue-700">
                                    <div><code class="bg-white px-1 py-0.5 rounded">{{'{{'}}prenom{{'}}'}}</code> → Prénom du client</div>
                                    <div><code class="bg-white px-1 py-0.5 rounded">{{'{{'}}nom{{'}}'}}</code> → Nom du client</div>
                                    <div><code class="bg-white px-1 py-0.5 rounded">{{'{{'}}nom_complet{{'}}'}}</code> → Nom complet</div>
                                    <div><code class="bg-white px-1 py-0.5 rounded">{{'{{'}}email{{'}}'}}</code> → Email du client</div>
                                    <div><code class="bg-white px-1 py-0.5 rounded">{{'{{'}}telephone{{'}}'}}</code> → Téléphone</div>
                                    <div><code class="bg-white px-1 py-0.5 rounded">{{'{{'}}date{{'}}'}}</code> → Date d'aujourd'hui</div>
                                </div>
                                <p class="mt-2 text-xs text-blue-600">
                                    <strong>Exemple :</strong> "Bonjour {{'{{'}}prenom{{'}}'}}, nous espérons que vous allez bien !"
                                </p>
                            </div>
                            <p class="mt-1 text-xs text-gray-500">
                                🎨 HTML autorisé : &lt;br&gt; &lt;b&gt; &lt;i&gt; &lt;u&gt; &lt;strong&gt; &lt;em&gt;
                            </p>
                        </div>

                        <!-- Options de destinataires -->
                        <div class="border-t border-gray-200 pt-6">
                            <h3 class="text-md font-medium text-gray-900 mb-4">Destinataires</h3>
                            
                            <!-- Envoyer à tous -->
                            <div class="mb-4">
                                <div class="flex items-center">
                                    {{ form_widget(form.sendToAll) }}
                                    {{ form_label(form.sendToAll, null, {'label_attr': {'class': 'ml-2 text-sm text-gray-700'}}) }}
                                </div>
                                <p class="mt-1 text-xs text-gray-500">Cochez cette case pour envoyer l'email à tous les clients ({{ total_clients }} personnes)</p>
                            </div>

                            <!-- Sélection manuelle -->
                            <div id="manual-selection" class="mb-4">
                                {{ form_label(form.recipients, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'}}) }}
                                {{ form_widget(form.recipients) }}
                                {{ form_errors(form.recipients) }}
                                <p class="mt-1 text-xs text-gray-500">
                                    Maintenez Ctrl (Cmd sur Mac) pour sélectionner plusieurs clients. 
                                    Cette option est ignorée si "Envoyer à tous" est coché.
                                </p>
                            </div>
                        </div>

                        <!-- Bouton d'envoi -->
                        <div class="border-t border-gray-200 pt-6">
                            {{ form_widget(form.submit) }}
                        </div>

                        {{ form_end(form) }}
                    </div>
                </div>

                <!-- Informations utiles -->
                <div class="mt-6 bg-blue-50 border border-blue-200 rounded-lg p-4">
                        <div class="flex-shrink-0">
                            <svg class="h-5 w-5 text-blue-400" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path>
                            </svg>
                        </div>
                        <div class="ml-3">
                            <h3 class="text-sm font-medium text-blue-800">Conseils d'utilisation</h3>
                            <div class="mt-2 text-sm text-blue-700">
                                <ul class="list-disc list-inside space-y-1">
                                    <li>Les emails sont envoyés depuis <strong>beellenailscare@beellenails.com</strong></li>
                                    <li>Personnalisez votre message en utilisant le nom du client qui sera automatiquement inséré</li>
                                    <li>Testez d'abord avec un petit groupe avant d'envoyer à tous les clients</li>
                                    <li>Les emails en erreur seront signalés individuellement</li>
                                </ul>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const sendToAllCheckbox = document.getElementById('bulk_email_sendToAll');
    const recipientsSelect = document.getElementById('bulk_email_recipients');
    
    function toggleRecipients() {
        if (sendToAllCheckbox.checked) {
            recipientsSelect.disabled = true;
            recipientsSelect.style.opacity = '0.5';
        } else {
            recipientsSelect.disabled = false;
            recipientsSelect.style.opacity = '1';
        }
    }
    
    sendToAllCheckbox.addEventListener('change', toggleRecipients);
    toggleRecipients(); // Initial state
});
</script>
{% endblock %}