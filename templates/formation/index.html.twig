{% extends 'base.html.twig' %}

{% block title %}Catalogue de Formations{% endblock %}

{% block body %}
<div class="min-h-screen bg-gray-50">
    <!-- Espaceur pour éviter que la navbar cache le contenu -->
    <div class="pt-24 md:pt-28"></div>

    <div class="px-4 py-6 sm:px-6 lg:px-8 max-w-7xl mx-auto">
        
        <!-- Header principal -->
        <div class="mb-8">
            <div class="text-center">
                <h1 class="text-3xl font-bold text-gray-900 mb-2">🎓 Be Elle Nails Académie</h1>
                <p class="text-lg text-gray-600">Formation professionnelle en prothésie ongulaire</p>
            </div>
        </div>

        <!-- Section À propos de l'Académie -->
        <div class="mb-8">
            <div class="bg-white rounded-lg shadow-sm overflow-hidden">
                <div class="px-6 py-4 border-b border-gray-200">
                    <h2 class="text-xl font-bold text-gray-900">📚 À propos de notre académie</h2>
                </div>
                <div class="p-6 space-y-4">
                    <p class="text-gray-600 leading-relaxed">
                        L'académie de formation <span class="text-pink-600 font-medium">Be Elle</span> dispense des formations professionnelles au métier de prothésiste ongulaire.
                    </p>
                    <p class="text-gray-600 leading-relaxed">
                        Elle offre également des modules de perfectionnement destinées aux professionnels exerçant et désirant parfaire leur connaissances afin de mieux répondre aux besoins leurs clientes et améliorer la qualité de leurs prestations.
                    </p>
                    <p class="text-gray-600 leading-relaxed">
                        Notre formation complète est destinée aux débutants désirant apprendre et exercer de façon professionnelle le métier de prothésiste ongulaire.
                    </p>
                    <p class="text-gray-600 leading-relaxed">
                        Les formations sont dispensées par <span class="text-pink-600 font-medium">Muriel AHODODE Epse ASSOGBA</span>, propriétaire de l'institut et prothésiste ongulaire spécialisé dans le traitement des ongles naturels. 
                        Formée aux quatre coins du monde auprès des formateurs les plus performants, elle transmet de façon professionnelle et détaillée son expérience et son expertise pendant chaque session de formation. Très portée sur le détail, la qualité et le professionnalisme elle continue de se former aux techniques les plus révolutionnaires et récentes pour former à son tour des professionnels qualifiés et actualisés en prothésie ongulaire.
                    </p>
                </div>
            </div>
        </div>

        <!-- Section Déroulement des sessions -->
        <div class="mb-8">
            <div class="bg-white rounded-lg shadow-sm overflow-hidden">
                <div class="px-6 py-4 border-b border-gray-200">
                    <h2 class="text-xl font-bold text-gray-900">⚙️ Comment se passent nos sessions de perfectionnement ?</h2>
                </div>
                <div class="p-6">
                    <div class="grid gap-4">
                        <div class="flex items-start">
                            <div class="flex-shrink-0 w-6 h-6 bg-pink-100 rounded-full flex items-center justify-center mr-3 mt-0.5">
                                <span class="text-xs font-semibold text-pink-600">1</span>
                            </div>
                            <p class="text-gray-600 leading-relaxed">La formation est composée de plusieurs leçons vidéos préenregistrées auxquelles vous avez accès après inscription.</p>
                        </div>
                        <div class="flex items-start">
                            <div class="flex-shrink-0 w-6 h-6 bg-pink-100 rounded-full flex items-center justify-center mr-3 mt-0.5">
                                <span class="text-xs font-semibold text-pink-600">2</span>
                            </div>
                            <p class="text-gray-600 leading-relaxed">Après l'inscription vous recevez un support pédagogique pour la session.</p>
                        </div>
                        <div class="flex items-start">
                            <div class="flex-shrink-0 w-6 h-6 bg-pink-100 rounded-full flex items-center justify-center mr-3 mt-0.5">
                                <span class="text-xs font-semibold text-pink-600">3</span>
                            </div>
                            <p class="text-gray-600 leading-relaxed">Des devoirs techniques à effectuer et à envoyer en correction sont donnés à la fin de chaque leçon.</p>
                        </div>
                        <div class="flex items-start">
                            <div class="flex-shrink-0 w-6 h-6 bg-pink-100 rounded-full flex items-center justify-center mr-3 mt-0.5">
                                <span class="text-xs font-semibold text-pink-600">4</span>
                            </div>
                            <p class="text-gray-600 leading-relaxed">Vous avez accès à un groupe télégram dans lequel la formatrice répond à toutes vos questions au quotidien.</p>
                        </div>
                        <div class="flex items-start">
                            <div class="flex-shrink-0 w-6 h-6 bg-pink-100 rounded-full flex items-center justify-center mr-3 mt-0.5">
                                <span class="text-xs font-semibold text-pink-600">5</span>
                            </div>
                            <p class="text-gray-600 leading-relaxed">Des cours en direct sont programmés avec la formatrice pour répondre à toutes vos questions ou consultations.</p>
                        </div>
                        <div class="flex items-start">
                            <div class="flex-shrink-0 w-6 h-6 bg-pink-100 rounded-full flex items-center justify-center mr-3 mt-0.5">
                                <span class="text-xs font-semibold text-pink-600">6</span>
                            </div>
                            <p class="text-gray-600 leading-relaxed">Une attestation de formation vous est délivrée à la fin de chaque module pour attester de vos performances pendant la formation.</p>
                        </div>
                    </div>
                    <div class="mt-6 p-4 bg-pink-50 rounded-lg">
                        <p class="text-pink-800 font-medium">ℹ️ Informations pratiques</p>
                        <p class="text-pink-700 mt-2">Chaque module se fait en 7 semaines et le matériel à utiliser pendant la pratique si vous êtes sur place est inclus dans le tarif.</p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Popular Formations Section -->
        {% if popularFormations is not empty %}
        <div class="mb-8">
            <div class="bg-gradient-to-r from-pink-500 to-purple-600 rounded-lg shadow-sm overflow-hidden">
                <div class="px-6 py-4 border-b border-pink-400">
                    <h2 class="text-xl font-bold text-white">⭐ Formations les mieux notées</h2>
                </div>
                <div class="p-6">
                    <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                        {% for formation in popularFormations %}
                            <div class="bg-white rounded-lg p-4 shadow-sm">
                                <h3 class="font-semibold text-gray-900 mb-2">{{ formation[0].nom }}</h3>
                                <div class="flex items-center mb-2">
                                    <div class="flex items-center">
                                        {% for i in 1..5 %}
                                            <svg class="w-4 h-4 {{ i <= formation.avgRating ? 'text-yellow-400' : 'text-gray-300' }}" fill="currentColor" viewBox="0 0 20 20">
                                                <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
                                            </svg>
                                        {% endfor %}
                                    </div>
                                    <span class="ml-2 text-sm text-gray-600">{{ formation.avgRating|number_format(1) }} ({{ formation.reviewCount }} avis)</span>
                                </div>
                                <a href="{{ path('app_formation_show', {id: formation[0].id}) }}" class="inline-flex items-center text-sm text-pink-600 hover:text-pink-700 font-medium">
                                    Voir la formation →
                                </a>
                            </div>
                        {% endfor %}
                    </div>
                </div>
            </div>
        </div>
        {% endif %}

        <div class="flex flex-col lg:flex-row gap-8">
            <!-- Sidebar Filters -->
            <div class="lg:w-80">
                <div class="bg-white rounded-lg shadow-sm overflow-hidden sticky top-6">
                    <div class="px-6 py-4 border-b border-gray-200">
                        <h3 class="text-lg font-bold text-gray-900">🔍 Filtrer les formations</h3>
                    </div>
                    
                    <form method="GET" action="{{ path('app_formation_index') }}" class="p-6 space-y-6" id="filter-form">
                        
                        <!-- Search -->
                        <div>
                            <label for="search" class="block text-sm font-medium text-gray-700 mb-2">Rechercher</label>
                            <input type="text" id="search" name="search" value="{{ currentFilters.search }}" 
                                   class="w-full border-gray-300 rounded-md shadow-sm focus:ring-pink-500 focus:border-pink-500"
                                   placeholder="Nom, description...">
                        </div>

                        <!-- Free/Paid Filter -->
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">Type de formation</label>
                            <select name="isFree" class="w-full border-gray-300 rounded-md shadow-sm focus:ring-pink-500 focus:border-pink-500">
                                <option value="">Toutes les formations</option>
                                <option value="1" {{ currentFilters.isFree == '1' ? 'selected' : '' }}>Formations gratuites</option>
                                <option value="0" {{ currentFilters.isFree == '0' ? 'selected' : '' }}>Formations payantes</option>
                            </select>
                        </div>

                        <!-- Theme Filter -->
                        {% if availableThemes is not empty %}
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">Thématique</label>
                            <select name="theme" class="w-full border-gray-300 rounded-md shadow-sm focus:ring-pink-500 focus:border-pink-500">
                                <option value="">Toutes les thématiques</option>
                                {% for theme in availableThemes %}
                                    <option value="{{ theme }}" {{ currentFilters.theme == theme ? 'selected' : '' }}>
                                        {{ theme|replace({'_': ' '})|title }}
                                    </option>
                                {% endfor %}
                            </select>
                        </div>
                        {% endif %}

                        <!-- Level Filter -->
                        {% if availableLevels is not empty %}
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">Niveau</label>
                            <select name="level" class="w-full border-gray-300 rounded-md shadow-sm focus:ring-pink-500 focus:border-pink-500">
                                <option value="">Tous les niveaux</option>
                                {% for level in availableLevels %}
                                    <option value="{{ level }}" {{ currentFilters.level == level ? 'selected' : '' }}>
                                        {{ level|capitalize }}
                                    </option>
                                {% endfor %}
                            </select>
                        </div>
                        {% endif %}

                        <!-- Duration Filter -->
                        {% if durationRange.max > 0 %}
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">Durée (en minutes)</label>
                            <div class="grid grid-cols-2 gap-2">
                                <input type="number" name="minDuration" value="{{ currentFilters.minDuration }}" 
                                       placeholder="Min" min="0" max="{{ durationRange.max }}"
                                       class="border-gray-300 rounded-md shadow-sm focus:ring-pink-500 focus:border-pink-500">
                                <input type="number" name="maxDuration" value="{{ currentFilters.maxDuration }}" 
                                       placeholder="Max" min="0" max="{{ durationRange.max }}"
                                       class="border-gray-300 rounded-md shadow-sm focus:ring-pink-500 focus:border-pink-500">
                            </div>
                            <p class="text-xs text-gray-500 mt-1">{{ durationRange.min }} - {{ durationRange.max }} min</p>
                        </div>
                        {% endif %}

                        <!-- Availability Filter -->
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">Disponibilité</label>
                            <select name="availability" class="w-full border-gray-300 rounded-md shadow-sm focus:ring-pink-500 focus:border-pink-500">
                                <option value="">Toutes</option>
                                <option value="available" {{ currentFilters.availability == 'available' ? 'selected' : '' }}>Disponibles maintenant</option>
                            </select>
                        </div>

                        <!-- Sort Order -->
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">Trier par</label>
                            <select name="orderBy" class="w-full border-gray-300 rounded-md shadow-sm focus:ring-pink-500 focus:border-pink-500">
                                <option value="newest" {{ currentFilters.orderBy == 'newest' ? 'selected' : '' }}>Plus récentes</option>
                                <option value="name" {{ currentFilters.orderBy == 'name' ? 'selected' : '' }}>Nom A-Z</option>
                                <option value="price_asc" {{ currentFilters.orderBy == 'price_asc' ? 'selected' : '' }}>Prix croissant</option>
                                <option value="price_desc" {{ currentFilters.orderBy == 'price_desc' ? 'selected' : '' }}>Prix décroissant</option>
                                <option value="duration_asc" {{ currentFilters.orderBy == 'duration_asc' ? 'selected' : '' }}>Durée courte</option>
                                <option value="duration_desc" {{ currentFilters.orderBy == 'duration_desc' ? 'selected' : '' }}>Durée longue</option>
                            </select>
                        </div>

                        <!-- Action Buttons -->
                        <div class="space-y-3">
                            <button type="submit" 
                                    class="w-full inline-flex items-center justify-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-pink-600 hover:bg-pink-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-pink-500">
                                🔍 Appliquer les filtres
                            </button>
                            
                            <a href="{{ path('app_formation_index') }}" 
                               class="w-full inline-flex items-center justify-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-pink-500">
                                ↻ Effacer les filtres
                            </a>
                        </div>
                    </form>
                </div>
            </div>

            <!-- Main Content -->
            <div class="flex-1">
                <!-- Results Header -->
                <div class="mb-6 flex items-center justify-between">
                    <div>
                        <h2 class="text-2xl font-bold text-gray-900">🎯 Nos formations</h2>
                        <p class="text-sm text-gray-600 mt-1">{{ formations|length }} formation(s) trouvée(s)</p>
                    </div>
                </div>
                
                {% if formations is not empty %}
                    <!-- Grid des formations -->
                    <div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-6">
                        {% for formation in formations %}
                            <!-- Card Formation -->
                            <div class="bg-white rounded-lg shadow-sm hover:shadow-lg transition-all duration-300 overflow-hidden border border-gray-100 hover:border-pink-200">
                                
                                <!-- Image -->
                                <div class="relative h-48 bg-gray-200">
                                    {% if formation.imageName %}
                                        <img src="{{ vich_uploader_asset(formation, 'image') }}" class="w-full h-full object-cover" alt="{{ formation.nom }}" />
                                    {% else %}
                                        <div class="w-full h-full flex items-center justify-center bg-gradient-to-br from-pink-100 to-purple-100">
                                            <span class="text-4xl text-pink-300">🎓</span>
                                        </div>
                                    {% endif %}
                                    
                                    <!-- Status Badge -->
                                    <div class="absolute top-3 left-3">
                                        {% if not formation.isAvailable %}
                                            <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-red-500 text-white">
                                                Non disponible
                                            </span>
                                        {% elseif formation.isFree %}
                                            <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-green-500 text-white">
                                                Gratuit
                                            </span>
                                        {% endif %}
                                    </div>
                                    
                                    <!-- Rating Badge -->
                                    {% if formation.averageRating %}
                                        <div class="absolute top-3 right-3">
                                            <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-yellow-100 text-yellow-800 shadow-sm">
                                                ⭐ {{ formation.averageRating }}
                                            </span>
                                        </div>
                                    {% endif %}
                                </div>

                                <!-- Contenu de la carte -->
                                <div class="p-6">
                                    <!-- Titre -->
                                    <h3 class="font-semibold text-gray-900 mb-3 leading-tight">{{ formation.nom }}</h3>
                                    
                                    <!-- Meta information -->
                                    <div class="space-y-2 mb-4">
                                        {% if formation.level or formation.theme %}
                                            <div class="flex flex-wrap gap-2">
                                                {% if formation.level %}
                                                    <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
                                                        {{ formation.level|capitalize }}
                                                    </span>
                                                {% endif %}
                                                {% if formation.theme %}
                                                    <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-purple-100 text-purple-800">
                                                        {{ formation.theme|replace({'_': ' '})|title }}
                                                    </span>
                                                {% endif %}
                                            </div>
                                        {% endif %}
                                        
                                        {% if formation.totalDuration > 0 %}
                                            <div class="flex items-center text-sm text-gray-500">
                                                <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
                                                </svg>
                                                {{ (formation.totalDuration / 60)|round }}h {{ formation.totalDuration % 60 }}min
                                            </div>
                                        {% endif %}

                                        {% if formation.modules|length > 0 %}
                                            <div class="flex items-center text-sm text-gray-500">
                                                <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"/>
                                                </svg>
                                                {{ formation.modules|length }} modules
                                            </div>
                                        {% endif %}
                                    </div>
                                    
                                    <!-- Description courte -->
                                    {% if formation.description %}
                                        <p class="text-sm text-gray-600 mb-4 line-clamp-2">{{ formation.description|slice(0, 120) }}{% if formation.description|length > 120 %}...{% endif %}</p>
                                    {% endif %}
                                    
                                    <!-- Price & CTA -->
                                    <div class="flex items-center justify-between">
                                        <div>
                                            {% if formation.isFree %}
                                                <span class="text-lg font-bold text-green-600">Gratuit</span>
                                            {% else %}
                                                <span class="text-lg font-bold text-gray-900">{{ formation.cout|number_format(0, ',', ' ') }} FCFA</span>
                                            {% endif %}
                                            
                                            {% if formation.approvedReviews|length > 0 %}
                                                <div class="text-xs text-gray-500 mt-1">{{ formation.approvedReviews|length }} avis</div>
                                            {% endif %}
                                        </div>
                                        
                                        <a href="{{ path('app_formation_show', {id: formation.id}) }}" 
                                           class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-pink-600 hover:bg-pink-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-pink-500 transition-colors">
                                            Voir plus
                                        </a>
                                    </div>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                {% else %}
                    <!-- État vide -->
                    <div class="bg-white rounded-lg shadow-sm p-12 text-center">
                        <svg class="w-16 h-16 mx-auto mb-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.746 0 3.332.477 4.5 1.253v13C19.832 18.477 18.246 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"/>
                        </svg>
                        <h3 class="text-lg font-medium text-gray-900 mb-2">Aucune formation trouvée</h3>
                        <p class="text-gray-500 mb-4">Essayez de modifier vos critères de recherche ou supprimez certains filtres.</p>
                        <a href="{{ path('app_formation_index') }}" 
                           class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-pink-600 hover:bg-pink-700">
                            Voir toutes les formations
                        </a>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
</div>

<style>
.line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
    // Auto-submit form on select change
    const selects = document.querySelectorAll('#filter-form select');
    selects.forEach(select => {
        select.addEventListener('change', function() {
            document.getElementById('filter-form').submit();
        });
    });

    // Submit form on Enter key for search input
    const searchInput = document.getElementById('search');
    searchInput?.addEventListener('keypress', function(e) {
        if (e.key === 'Enter') {
            e.preventDefault();
            document.getElementById('filter-form').submit();
        }
    });
});
</script>
{% endblock %}