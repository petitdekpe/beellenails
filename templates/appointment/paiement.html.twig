{% extends 'base.html.twig' %}

{% block body %}
{% set choixPrestationData = app.session.get('choixPrestation_data') %}
{% set choixDateData = app.session.get('choixDate_data') %}
<div class="relative overflow-hidden w-full min-h-[90px] bg-white rounded-lg"></div>
<h1 class="text-2xl font-bold my-5 text-center">Faites votre choix de Prestation</h1>
    <h2 class="text-xl font-bold my-5 text-justify mx-8">Bonjour {{app.user.Prenom}} {{app.user.Nom}},</h2>
    <h2 class="text-xl font-semibold text-justify mx-8">Vous avez choisi de faire : </h2>
    <p class="mx-8">La prestation {{ choixPrestationData.prestation }}, le {{ choixDateData.day|date("d/m/Y") }} à {{ choixDateData.creneau }}. Le prix de cette prestation est de {{ choixPrestationData.prestation.price }} FCFA.</p>
    <p class="mx-8 mt-5">Pour une prestation optimale, nous demandons d'ajouter une photo de vos mains ou pieds au naturel afin que nous puissions les analyser.</p>

    {{ form_start(form) }}
    <div class="mt-12 mb-12 mx-8 flex flex-col justify-center content-center">
        <ol class="relative border-s border-gray-200">
                   <li class="ms-6">
        <span class="absolute flex items-center justify-center w-6 h-6 bg-blue-100 rounded-full -start-3 ring-8 ring-white">
            <svg class="w-2.5 h-2.5 text-blue-800" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 20 20">
                <path d="M20 4a2 2 0 0 0-2-2h-2V1a1 1 0 0 0-2 0v1h-3V1a1 1 0 0 0-2 0v1H6V1a1 1 0 0 0-2 0v1H2a2 2 0 0 0-2 2v2h20V4ZM0 18a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2V8H0v10Zm5-8h10a1 1 0 0 1 0 2H5a1 1 0 0 1 0-2Z"/>
            </svg>
        </span>
        <h3 class="mb-1 text-lg font-semibold text-gray-900">{{form_label(form.image)}}</h3>
        <time class="block mb-2 text-sm font-normal leading-none text-gray-400"></time>
        <p class="text-base font-normal text-gray-500"> {{ form_widget(form.image) }} </p>
        <p> {{ form_help(form.image) }} </p>
    </li>
        </ol>
         {{ form_row(form._token) }}
    </div>
    <div class="flex justify-center">
        <button type="submit" class="btn btn-primary my-5 text-white bg-pink-700 hover:bg-pink-500 focus:ring-4 font-medium rounded-lg text-sm w-1/2 sm:w-auto px-5 py-2.5 text-center">Suivant</button>
    </div>
    
    {{ form_end(form, {'render_rest': false}) }}


{% endblock %}