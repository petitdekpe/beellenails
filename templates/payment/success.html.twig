{% extends 'base.html.twig' %}

{% block title %}Paiement réussi{% endblock %}

{% block body %}
<div class="min-h-screen bg-gray-50 py-8 pt-32">
    <div class="max-w-2xl mx-auto px-4">
        <div class="bg-white rounded-xl shadow-lg overflow-hidden">
            <!-- Success Header -->
            <div class="bg-gradient-to-r from-green-500 to-green-600 px-6 py-8 text-white">
                <div class="text-center">
                    <div class="inline-flex items-center justify-center w-16 h-16 bg-white bg-opacity-20 rounded-full mb-4">
                        <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                        </svg>
                    </div>
                    <h1 class="text-3xl font-bold mb-2">Paiement réussi !</h1>
                    <p class="text-green-100">Votre transaction a été traitée avec succès</p>
                </div>
            </div>

            <div class="p-6">
                <!-- Transaction Summary -->
                <div class="bg-green-50 border border-green-200 rounded-lg p-4 mb-6">
                    <h3 class="font-semibold text-green-900 mb-3 flex items-center">
                        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                        Transaction confirmée
                    </h3>
                    
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-sm">
                        <div>
                            <span class="text-green-700">Référence:</span>
                            <span class="font-mono font-medium block text-green-900">{{ payment.reference }}</span>
                        </div>
                        <div>
                            <span class="text-green-700">Montant:</span>
                            <span class="font-bold text-lg block text-green-900">{{ payment.amount|number_format(0, ',', ' ') }} {{ payment.currency }}</span>
                        </div>
                        <div>
                            <span class="text-green-700">Date:</span>
                            <span class="font-medium block text-green-900">{{ payment.updatedAt|date('d/m/Y à H:i') }}</span>
                        </div>
                        <div>
                            <span class="text-green-700">Provider:</span>
                            <span class="font-medium block text-green-900">
                                {% if payment.provider == 'feexpay' %}
                                    💸 FeexPay
                                {% elseif payment.provider == 'fedapay' %}
                                    🏦 FedaPay
                                {% else %}
                                    {{ payment.provider|title }}
                                {% endif %}
                            </span>
                        </div>
                    </div>
                </div>

                <!-- Next Steps -->
                {% if entity %}
                    <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 mb-6">
                        <h4 class="font-semibold text-blue-900 mb-3">Prochaines étapes</h4>
                        
                        {% if payment.entityType == 'rendezvous' %}
                            <div class="text-sm text-blue-800 space-y-2">
                                <div class="flex items-start">
                                    <svg class="w-4 h-4 mt-0.5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                                    </svg>
                                    <span>Un email de confirmation vous a été envoyé avec tous les détails de votre rendez-vous</span>
                                </div>
                                <div class="flex items-start">
                                    <svg class="w-4 h-4 mt-0.5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3a2 2 0 012-2h4a2 2 0 012 2v4m-6 4v8a2 2 0 002 2h4a2 2 0 002-2v-8"></path>
                                    </svg>
                                    <span>Votre rendez-vous est maintenant confirmé et réservé</span>
                                </div>
                                <div class="flex items-start">
                                    <svg class="w-4 h-4 mt-0.5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                                    </svg>
                                    <span>Vous recevrez un rappel 24h avant votre rendez-vous</span>
                                </div>
                            </div>
                        {% elseif payment.entityType == 'formation' %}
                            <div class="text-sm text-blue-800 space-y-2">
                                <div class="flex items-start">
                                    <svg class="w-4 h-4 mt-0.5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.746 0 3.332.477 4.5 1.253v13C20.168 18.477 18.582 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path>
                                    </svg>
                                    <span>Votre inscription à la formation a été confirmée</span>
                                </div>
                                <div class="flex items-start">
                                    <svg class="w-4 h-4 mt-0.5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 7a2 2 0 012 2m4 0a6 6 0 01-7.743 5.743L11 17H9v2H7v2H4a1 1 0 01-1-1v-2.586a1 1 0 01.293-.707l5.964-5.964A6 6 0 1121 9z"></path>
                                    </svg>
                                    <span>Vous recevrez vos accès de connexion par email</span>
                                </div>
                                <div class="flex items-start">
                                    <svg class="w-4 h-4 mt-0.5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                                    </svg>
                                    <span>Les détails de la formation vous seront envoyés par email</span>
                                </div>
                            </div>
                        {% endif %}
                    </div>
                {% endif %}

                <!-- Actions -->
                <div class="space-y-3">
                    {% if payment.entityType == 'rendezvous' %}
                        <a href="{{ path('app_dashboard_rendezvous') }}" 
                           class="block w-full bg-pink-600 hover:bg-pink-700 text-white text-center font-semibold py-3 px-6 rounded-lg transition-colors">
                            Voir mes rendez-vous
                        </a>
                    {% elseif payment.entityType == 'formation' %}
                        <a href="{{ path('formation_index') }}" 
                           class="block w-full bg-pink-600 hover:bg-pink-700 text-white text-center font-semibold py-3 px-6 rounded-lg transition-colors">
                            Accéder à mes formations
                        </a>
                    {% endif %}
                    
                    <a href="{{ path('app_home') }}" 
                       class="block w-full bg-gray-100 hover:bg-gray-200 text-gray-700 text-center font-semibold py-3 px-6 rounded-lg transition-colors">
                        Retour à l'accueil
                    </a>
                    
                    <button onclick="window.print()" 
                            class="block w-full bg-white hover:bg-gray-50 text-gray-700 text-center font-medium py-2 px-6 rounded-lg border border-gray-300 transition-colors">
                        <svg class="w-4 h-4 inline mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 17h2a2 2 0 002-2v-4a2 2 0 00-2-2H5a2 2 0 00-2 2v4a2 2 0 002 2h2m2 4h6a2 2 0 002-2v-4a2 2 0 00-2-2H9a2 2 0 00-2 2v4a2 2 0 002 2zm8-12V5a2 2 0 00-2-2H9a2 2 0 00-2 2v4h10z"></path>
                        </svg>
                        Imprimer le reçu
                    </button>
                </div>
            </div>
        </div>

        <!-- Customer Support -->
        <div class="mt-6 bg-white rounded-lg p-4 border border-gray-200">
            <div class="text-center">
                <h4 class="font-medium text-gray-900 mb-2">Questions ou problèmes ?</h4>
                <p class="text-sm text-gray-600 mb-3">
                    Notre équipe est là pour vous aider
                </p>
                <div class="flex justify-center space-x-4 text-sm">
                    <a href="mailto:beellenailscare@beellenails.com" 
                       class="text-pink-600 hover:text-pink-700 font-medium">
                        📧 Email
                    </a>
                    <span class="text-gray-300">|</span>
                    <a href="tel:+22997123456" 
                       class="text-pink-600 hover:text-pink-700 font-medium">
                        📞 Téléphone
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}