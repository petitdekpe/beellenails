{% extends 'base.html.twig' %}

{% block title %}Paiement FeexPay{% endblock %}

{% block body %}
<div class="min-h-screen bg-gray-50 py-8">
    <div class="max-w-2xl mx-auto px-4">
        <div class="bg-white rounded-xl shadow-lg overflow-hidden">
            <!-- Header -->
            <div class="bg-gradient-to-r from-purple-600 to-purple-700 px-6 py-8 text-white">
                <h1 class="text-2xl font-bold mb-2">Paiement Mobile Money</h1>
                <p class="text-purple-100">Saisissez vos informations pour procéder au paiement</p>
            </div>

            <div class="p-6">
                <!-- Payment Summary -->
                <div class="bg-gray-50 rounded-lg p-4 mb-6">
                    <h3 class="font-semibold text-gray-900 mb-3">Récapitulatif du paiement</h3>
                    <div class="space-y-2">
                        <div class="flex justify-between text-sm">
                            <span class="text-gray-600">Description:</span>
                            <span class="font-medium">{{ description }}</span>
                        </div>
                        <div class="flex justify-between text-sm">
                            <span class="text-gray-600">Client:</span>
                            <span class="font-medium">{{ user.fullName }}</span>
                        </div>
                        {% if entity.entityType == 'rendezvous' and entity.prestation %}
                            <div class="flex justify-between text-sm">
                                <span class="text-gray-600">Prestation:</span>
                                <span class="font-medium">{{ entity.prestation.title }}</span>
                            </div>
                        {% endif %}
                        {% if entity.entityType == 'formation' %}
                            <div class="flex justify-between text-sm">
                                <span class="text-gray-600">Formation:</span>
                                <span class="font-medium">{{ entity.nom }}</span>
                            </div>
                        {% endif %}
                        <hr class="my-2">
                        <div class="flex justify-between font-semibold text-lg">
                            <span>Montant à payer:</span>
                            <span class="text-purple-600">{{ amount|number_format(0, ',', ' ') }} XOF</span>
                        </div>
                    </div>
                </div>

                {% if error %}
                    <div class="bg-red-50 border border-red-200 rounded-lg p-4 mb-6">
                        <div class="flex items-center">
                            <svg class="w-5 h-5 text-red-400 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                            </svg>
                            <span class="text-red-800 font-medium">{{ error }}</span>
                        </div>
                    </div>
                {% endif %}

                <!-- Payment Form -->
                {{ form_start(form, {'attr': {'class': 'space-y-6'}}) }}
                
                <div>
                    {{ form_label(form.phone, 'Numéro de téléphone', {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-2'}}) }}
                    {{ form_widget(form.phone, {'attr': {
                        'class': 'w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500',
                        'placeholder': 'Ex: 22951234567'
                    }}) }}
                    {{ form_errors(form.phone) }}
                    <p class="text-sm text-gray-500 mt-1">Numéro associé à votre compte Mobile Money</p>
                </div>

                <div>
                    {{ form_label(form.operator, 'Opérateur', {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-2'}}) }}
                    {{ form_widget(form.operator, {'attr': {
                        'class': 'w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500'
                    }}) }}
                    {{ form_errors(form.operator) }}
                </div>

                <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
                    <div class="flex items-start">
                        <svg class="w-5 h-5 text-blue-400 mt-0.5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                        <div class="text-sm text-blue-800">
                            <p class="font-medium mb-1">Instructions de paiement :</p>
                            <ul class="list-disc list-inside space-y-1">
                                <li>Assurez-vous que votre compte Mobile Money dispose du solde suffisant</li>
                                <li>Vous recevrez un message de confirmation sur votre téléphone</li>
                                <li>Suivez les instructions pour valider le paiement</li>
                                <li>Ne fermez pas cette page pendant le processus</li>
                            </ul>
                        </div>
                    </div>
                </div>

                <div class="flex space-x-4 pt-6">
                    <button type="button" 
                            onclick="history.back()" 
                            class="flex-1 px-6 py-3 border border-gray-300 rounded-lg text-gray-700 font-semibold hover:bg-gray-50 transition-colors">
                        Retour
                    </button>
                    
                    <button type="submit" 
                            class="flex-1 bg-purple-600 hover:bg-purple-700 text-white font-semibold py-3 px-6 rounded-lg transition-colors flex items-center justify-center">
                        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"></path>
                        </svg>
                        Payer {{ amount|number_format(0, ',', ' ') }} XOF
                    </button>
                </div>

                {{ form_end(form) }}
            </div>
        </div>

        <!-- Security Notice -->
        <div class="mt-6 bg-white rounded-lg p-4 border border-gray-200">
            <div class="flex items-center text-sm text-gray-600">
                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.031 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path>
                </svg>
                <span>Paiement sécurisé par FeexPay. Vos données sont protégées.</span>
            </div>
        </div>
    </div>
</div>
{% endblock %}